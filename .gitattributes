# Keep the verified dataset bundle byte-identical across Windows/Linux checkouts.
# Large generated snapshots are checked with verify_outputs.py instead of line diffs.
/frontend/public/data/* -text -diff
