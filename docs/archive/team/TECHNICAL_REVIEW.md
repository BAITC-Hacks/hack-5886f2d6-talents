# Приоритет до сдачи: техническая проверка

> Архив этапа разработки. Числа, версии и статусы относятся к указанным ниже
> проверкам. Текущие инструкции: [документация](../../README.md).

План уточнён по положению, переданному Артуром 23.09.2026, и ТЗ в `TechTask/`.
Положение описывает этапы; конкретные технические баллы берём из ТЗ нашего кейса.

## Текущий статус: fbec33f, C++ 1.2.0

Изменения Савелия приняты. Показ `next_actions`, карточка направленной связи
с суммой и количеством переводов и полный комплект C++ 1.2.0 включены в проект.
Принят [финальный отчёт Армана](../../../team/arman/FINAL_RUN.md): **110 Python-тестов,
34 теста ядра и 22 теста UI** прошли, production-сборка успешна. Новая копия
`9e5eff6` с новыми venv, Release-сборкой и node_modules обновлена tracked-файлами
до `859b33f`; после объединения UI проверен `2e2f385`. Затронутые проверки
повторены. Пересчёт с подготовкой и проверкой пакета в этой копии занял **6.324 с**.
Это последовательное обновление чистой базы, а не повторная установка всех
зависимостей с нуля на каждом коммите; исходники для запуска не исправлялись.

На ноутбуке Артура после принятия `fbec33f` повторно прошли **110 Python-тестов,
22 теста UI** и production-сборка; **34 теста ядра** прошли ранее в этой приёмке,
исходники ядра в обновлении Армана не менялись. Полный проверочный пересчёт
принятой версии занял **6.627 с**, `failures=[]`, `warnings=[]`.
Проверены хэши поставки и исходных данных. Комплект содержит 2248 клиентов,
3119 связей, 88 кластеров и топ-20; SHA-256 manifest:
`9c755304021b14dbe3b484de8e1cfd77cde315e695c6b46f11851ff505acdf27`.

Часть Армана по воспроизводимости завершена. Остаются командная приёмка и
ручной цикл экспорта старого/нового расчёта. Зафиксированная
на Windows ошибка замены файла при работающем Vite не объявляется устранённой;
порядок повторного запуска описан в [frontend/README.md](../../../frontend/README.md).

Исторические замеры и проверки версии 1.1.0 ниже сохранены для прослеживаемости;
их числа тестов, времена и отметки о готовности относятся к указанным коммитам.
Текущие возможности и дальнейшая приёмка — в [FINISH_PLAN.md](FINISH_PLAN.md).

## Этапы

- 23 сентября, 13:00–18:00 по Астане — соревновательная часть. По п. 5.4.13
  итоговая версия фиксируется в 18:00 независимо от времени блокировки платформы.
- 24–28 сентября — техническая проверка решений.
- 29 сентября — Demo Day для прошедших отбор.
- По п. 5.4.14 оценивается зафиксированная итоговая версия и на Demo Day:
  доработки после окончания соревновательной части не учитываются.

Сейчас необходимо завершить командную приёмку на демонстрационном ноутбуке.
Подготовка выступления остаётся на следующий этап; предусмотренное ТЗ пятиминутное
демо должно быть выполнимо на уже сданной версии.

## Критерии технического отбора из ТЗ

| Критерий | Максимум | Что предъявляем в репозитории |
|---|---:|---|
| Соответствие задаче и работоспособность | 25 | Полный расчёт, три CSV, экран с направлением потоков и поиском |
| Техническая реализация | 25 | Проверки данных, C++-правила, графовые признаки, Louvain, согласованный контракт |
| README и воспроизводимость | 25 | Установка, сборка, одна команда расчёта, проверка результата и запуск UI |
| Ценность и применимость | 15 | Топ с объяснениями, карточка клиента, ограничения и следующий шаг проверки |
| Потенциал развития и оригинальность | 10 | Обоснованные дополнительные признаки и план масштабирования |

Это веса критериев, а не оценка уже набранных командой баллов.
Таблица Demo Day из п. 5.7.2 относится к другому этапу.

## Историческая проверка обязательных функций: версия 1.1.0

Изменения аналитического пайплайна `f043aa3` интегрированы с интерфейсом без
конфликтов. Прошли 78 Python-тестов, 28 тестов C++ и 7 тестов frontend,
production-сборка UI. Полный проверенный пересчёт
`python prepare_ui_data.py --out engine/test-output/frontend-compat` занял
3.391 секунды (worker — 1.969 секунды); хэши комплекта проверены.
Актуальные подробности: [frontend/VERIFICATION.md](../frontend/VERIFICATION.md).

| Требование ТЗ | Подтверждение | Что осталось |
|---|---|---|
| Один запуск от Parquet до трёх CSV, <5 минут | Полный пересчёт и проверка комплекта — 3.391 секунды | Повторить на втором ноутбуке и перед фиксацией итоговой версии |
| Роль, score и evidence для всех 2248 узлов | Тесты ядра и пайплайна, проверка полного набора gid; файлы в `frontend/public/data/` | Обновлять весь комплект при изменении расчёта |
| Объяснимые роли и пороги | `engine/README.md`, `meta.config`, features и priority_breakdown; объяснения и вклады отображаются в карточке | Совместная сверка контрольных карточек участниками |
| Кластеры и сводка по ним | 88 кластеров, `clusters.csv`, внутренние суммы и новые hypothesis; выбранный кластер отображается в UI | Сверить на финальной демонстрационной версии |
| Топ >=20 и схема со стрелками, ролями и поиском gid | Готовый React/Vite/Cytoscape UI: топ-20, направленный граф, фильтры, точный поиск и три CSV | Командная приёмка на втором ноутбуке |

Примеры в `examples/` синтетические и содержат четыре узла.
Они не заменяют обязательные выгрузки на полном датасете. Проверка интерфейса
описана отдельно от проверки ядра и доставки файлов. Второй ноутбук и другие
ОС в текущей проверке не подтверждены; общая командная приёмка ещё предстоит.

## Следующий результат каждого участника

### Артур — совместная проверка объяснений

Пункты аналитического этапа выполнены для `c7f7bb4`:
[исторический отчёт](../engine/CLEAN_CHECK.md). Новая сборка, 49 + 28 тестов
и полный запуск прошли; проверяющий прогон тогда занял 2.914 секунды.
Актуальные общие проверки указаны выше, UI уже интегрирован.

1. Вместе с Савелием пройти произвольные gid и четыре контрольных случая
   из `DEMO_CASES.md`, включая границу обхода и изолированного seed.
2. Сверить роли, оценки, evidence, why, role_candidates, warnings и вклады
   приоритета с graph.json и result.json финального комплекта.
3. В общей проверке на втором ноутбуке зафиксировать команды, ОС и результат;
   не переносить выводы проверки одного компьютера на другие окружения.

### Арман — финальный комплект данных и воспроизводимость

Комплект C++ 1.1.0 включён в Git в `frontend/public/data/` и подключён к UI:
2248 узлов, 3119 связей, 88 кластеров, топ-20, три CSV, graph.json,
validation.json и manifest; input/result оставлены для независимой сверки.
Повторная доставка — `python prepare_ui_data.py`, проверка без пересчёта —
`python verify_outputs.py`. Статические URL и команды описаны в
[frontend/README.md](../../../frontend/README.md).

1. При изменении расчёта обновлять весь комплект одним запуском. Не смешивать
   JSON, CSV и manifest разных прогонов и не подменять полные файлы примерами.
2. Перед сдачей сверить хэши финального комплекта и его совпадение с данными UI.
   Стандартная `out/` исключена из Git; файлы сдачи находятся в `frontend/public/data/`.
3. На втором ноутбуке повторить установку зависимостей, Release-сборку C++,
   полный расчёт и запуск интерфейса; сохранить фактический результат.

### Савелий — приёмка готового экрана

Реализованы топ, карточка, направленные связи, раскраска по роли/кластеру,
фильтры, точный поиск строкового gid включая изолированные узлы, объяснения,
ограничения и скачивание трёх исходных CSV. Приложение работает локально
без CDN и обязательных внешних API. Запуск из корня репозитория:

```sh
cd frontend
npm ci
npm run dev
```

1. Вместе с Артуром и Арманом повторить контрольные случаи на финальном комплекте.
2. На втором ноутбуке подтвердить граф, поиск, карточки и скачивание кнопками UI.
3. Пройти [пятиминутный сценарий](../../../frontend/DEMO.md) и зафиксировать финальный
   код и данные. Детали выполненных проверок — в [отчёте](../frontend/VERIFICATION.md).

## Перед фиксацией итоговой версии

- Все обязательные исходники и материалы отправлены в репозиторий организаторов;
  история разработки сохранена. Положение требует промежуточный результат каждый час.
- README содержит назначение, архитектуру, технологии, установку, запуск,
  зависимости, параметры окружения и основной сценарий проверки (п. 5.4.15).
- Новый пользователь может получить три полных CSV и открыть UI по этим командам.
  Незапускаемый по README проект не проходит отбор (п. 5.4.16).
- Сторонние материалы раскрыты (п. 5.4.4); AI-инструменты разработки разрешены
  (п. 5.4.12). Использование AI при разработке не означает наличия обученной
  AI-модели внутри продукта.
- Финальный коммит и его выгрузки соответствуют показываемой версии решения.
