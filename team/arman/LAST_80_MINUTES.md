# Арман — проверка эксперимента и надёжная публикация

Реализовать до 17:10, интеграция 17:10–17:25; после 17:25 новые функции не добавлять.
Источник контракта: [RESILIENCE_CONTRACT.md](../RESILIENCE_CONTRACT.md).

Статус Python-части: реализация и проверки готовы; **324 passed, 9 skipped**.
Девять проверок нового C++ и пересчёт общего пакета ждут публикации C++ 1.3.0
и UI. Текущий пакет 1.2.0 сохранён целиком и проверен. Подробности, SHA и
независимые числа полного датасета: [RESILIENCE_CHECK.md](RESILIENCE_CHECK.md).

## 1. Структурный эксперимент

Артур добавляет result.meta.resilience в C++ 1.3.0. Поле уже автоматически
проходит через engine_meta в graph.meta; проверь это, не создавай второй независимый
формат. Схему C++ редактирует Артур. Валидируй структуру, присутствие и точное
сохранение метаданных в verify_outputs; старый пакет без поля должен приниматься.

Независимо воспроизведи 8 сценариев на NetworkX: направленные рёбра сохраняют
суммы, компоненты считаются как weak. Для изолированных исключить self-loops
из внешних связей. Проверь все числа и знаменатели на полном датасете и крайних
синтетических графах. Эти расчёты — проверочный эталон, рабочие значения поступают
из C++. Основные CSV, роли и scores должны остаться прежними.

## 2. Windows-блокировка записи

Добавь ограниченный retry только для временных ошибок доступа/совместного
использования при os.replace (Windows 5/32/33), с короткой задержкой и общим
бюджетом публикации не более 3 секунд и оставшегося времени supervisor.
Постоянная ошибка завершает запуск с ненулевым кодом; manifest публикуется
последним. Не выдавай частичный комплект за complete и не ослабляй SHA-проверки.
Проверки: временная ошибка→успех, постоянная→ошибка, другая I/O-ошибка→сразу ошибка,
лимит времени, сохранение порядка публикации. Не менять права файлов или ОС.

## 3. Общий пакет

После получения C++ и UI собери ядро, один раз пересчитай весь public/data,
запусти проверку исходных данных/бинарника/артефактов и UI-тесты. При конфликте
generated-файлов выбирай или пересчитывай весь пакет, не склеивай JSON текстом.
Опубликуй проверенный комплект и короткий отчёт с SHA, временем и фактическими
числами структурного эксперимента. Если живой запуск с Vite остаётся нестабилен,
сохрани рабочий порядок остановка→пересчёт→запуск из DEMO.md.

Мои файлы: pipeline.py, hackalem, verify_outputs.py, Python tests, общий CONTRACT.md,
frontend/public/data, team/arman. Ядро и UI не переписывать.
