# Артур — следующие действия по C++ и интеграции

Python ad6b1d5 адаптирован к C++ 1.1.0. Полный контрольный прогон на 2248 узлах
занял 3.114 секунды; прошли 49 тестов Python и 28 тестов движка. Все поля ядра
сохраняются в graph.json для UI. Актуальный план команды: [NEXT_STEPS.md](NEXT_STEPS.md).

Интеграция опубликована в `main`, коммит `c7f7bb4`.
Следующая задача — сборка из новой копии репозитория, самостоятельная проверка
по README и сверка карточек после первого коммита интерфейса.
Demo Day проходит 29 сентября; до окончания разработки приоритет —
[техническая сдача](TECHNICAL_REVIEW.md).

Проверка новой копии `c7f7bb4` завершена: сборка с нуля, 49 + 28 тестов,
полный проверяющий прогон за 2.914 секунды. [Подробный отчёт](../engine/CLEAN_CHECK.md).
Самостоятельная проверка и компоненты описаны в корневом README и THIRD_PARTY.md.
Теперь ожидаем UI для совместной проверки карточек.

Повторный полный аудит с коммитом Армана `4a9c671`:
[78 + 28 тестов и сверка требований](../engine/TZ_AUDIT.md).
Данные для UI пересчитаны и сохранены в `frontend/public/data/`.

Контракт `engine/CONTRACT.md` — источник истины для C++; корневой `CONTRACT.md`
описывает совместимый Python-экспорт и формат UI. Согласованная интеграция
находится в общем репозитории.

## Что уже готово у Армана

Проверки Parquet, сохранение всех 2248 узлов (включая 19 изолированных seed), метрики стартера, Louvain с seed=42 и суммированием встречных рёбер, три обязательных CSV, graph.json, строгая проверка результата, общий таймаут 300 секунд. Получено 88 сообществ. Реальные оценки C++ проходят до CSV и UI без пересчёта.

Полный запуск после установки зависимостей и сборки ядра: `python pipeline.py`. По умолчанию результат записывается в `out/`. Python-demo остаётся отдельным явным режимом `--demo`.

## Что Арман изменил для подключения C++

1. Вызов `engine input.json result.json`, с передачей `--config` при необходимости.
2. Плоские метрики input.json, как в твоей JSON Schema; gid остаются строками канонического int64.
3. Чтение `engine_version`, проверка совпадения cluster_id и полного набора gid.
4. Сохранение features, role_candidates, priority_breakdown, warnings и meta.config в graph.json. Правила, роли и приоритеты C++ не пересчитываются в Python.
5. Плоские узлы UI, `id=src+":"+dst` у рёбер, `meta.is_demo=false` для полного прогона C++. Сортировка при равном приоритете — по числовому gid, как у ядра.
6. Реальная сборка, тесты и прогон до CSV/graph.json, обновление README и примеров.

## Твои следующие действия

- Сохрани текущий контракт C++ стабильным на время подключения. Несовместимые изменения названий/типов полей или CLI сначала согласовать с Арманом.
- Проверь сборку и расчёт из новой копии GitHub-репозитория с новым окружением;
  сохрани отчёт и команды для экспертов. Если у Армана возникнет проблема
  с компилятором, передай проверенную сборку с версией исходного коммита.
- После подключения UI сверь с Савелием карточки из `DEMO_CASES.md`: оба score,
  evidence, why, priority_breakdown, альтернативные роли и предупреждения.
  `role_score` — сила признаков, не вероятность виновности.
- Перед сдачей проверь, что UI и полные CSV взяты из одного успешного прогона,
  а инструкция позволяет повторить расчёт без личных аккаунтов и пояснений команды.
- При изменении правил обновляй `engine/config/default.json`, тесты, README и engine_version вместе; не зашивай специальные gid.

## Приёмка совместного прогона

Все 2248 gid ровно один раз; 19 изолированных сохранены; у всех есть непустые объяснения. На границе depth=4 отсутствие исходящих не даёт terminal. Оценки 0–1, суммы и количества согласованы с рёбрами, результаты C++ и выгрузки совпадают по gid. Повторный запуск воспроизводим и укладывается в 5 минут.

Маленькие файлы `examples/` пересчитаны настоящим C++ и остаются синтетическими примерами (`meta.is_demo=true`). `engine/examples/` — канонические примеры автора ядра. На полном датасете итоговый graph.json имеет `meta.is_demo=false`.
