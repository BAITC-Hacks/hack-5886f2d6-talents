# Артур — C++: устойчивость сети

**C++ выполнен:** версия 1.3.0, 43 теста; расчёт на 2248 клиентах и независимая
сверка всех сценариев. [Результаты и польза](../../docs/EVIDENCE.md).
Осталась совместная приёмка пакета и UI после работы Армана и Савелия.
Общая отсечка функций — 17:25, финальный push — 17:45.
Источник контракта: [RESILIENCE_CONTRACT.md](../RESILIENCE_CONTRACT.md).

Мой стек C++17/STL/CMake/nlohmann JSON. Текущая версия 1.2.0 и её правила работают.
Добавь необязательный для старых потребителей result.meta.resilience; новая версия
ядра — 1.3.0. Baseline и 8 сценариев исключения top-1/3/5/10 по priority/volume.
Подсчёт слабых компонент, изолированных, крупнейшей компоненты, оставшихся рёбер
и затронутых сумм — строго по общему контракту. Не привязывать правила к gid.

Используй уже разобранный граф, суммы и готовый порядок priority. Достаточны
списки смежности/DSU/BFS; новые библиотеки и входные поля не нужны. Сложность
добавленного блока O(N log N + 9(N+M)). Старые роли, scores, evidence, next_actions,
топ и конфигурацию не менять. Допиши схему meta.resilience и документацию.

Мои файлы: engine/src, engine/CMakeLists.txt, engine/schemas/result.schema.json,
engine/tests, engine/README.md, engine/CONTRACT.md, engine/examples.
Python, UI и frontend/public/data оставлены тиммейтам. Результат для интеграции
сразу передать в GitHub; после прогона Армана проверить новый блок и сохранность CSV.

Проверки: пункты приёмки контракта, все тесты ядра, полный датасет; объяснить
результат priority против volume как сравнение стратегий, без обещания разрушения
реальной группы. Контракт реализован в ядре; статус поставки UI указан выше.
